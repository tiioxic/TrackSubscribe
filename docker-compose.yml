services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./server/database.sqlite:/app/server/database.sqlite
    restart: unless-stopped
    environment:
      - PORT=3001
      - NODE_ENV=production
